# macOS
.DS_Store

# Claude Artifact preview file — not part of the deployed site
# (large, contains an embedded base64 copy of the logo for preview-only purposes)
artifact-preview.html

# Local test artifacts
*.log

# OGP card build source (kept locally for regenerating og-image.jpg later;
# not a page of the site itself, see README)
og-card-source.html

# Cloudflare wrangler local cache (contains account email/ID)
.wrangler/
